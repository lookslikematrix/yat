pipeline {
    agent any

    stages {
        stage('🔶 pre-commit') {
            steps {
                script {
                    sh '''#!/bin/bash
                        echo "🔶 pre-commit"
                    '''
                }
            }
        }
    }
}
