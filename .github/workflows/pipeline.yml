---
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  pre_commit:
    name: "🔶 pre-commit"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: "🔶 pre-commit"
        run: |
          echo "🔶 pre-commit"
